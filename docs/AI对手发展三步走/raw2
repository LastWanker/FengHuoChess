具体到模型，使用什么模型呢？
已思考 27s

具体到模型，我的建议很明确：

第一版用“小型棋盘 CNN（卷积网络）+ policy head（策略头）”，不要用 Transformer，不要先上 MCTS，不要先搞大而全双头 AlphaZero 套餐。

原因很朴素。你的输入本质上是一个 15×15 棋盘空间结构，而且规则是“局面 + 阶段 + 模式”共同决定动作。你现在的系统也已经天然适配“状态 -> 合法动作 -> 选一步”这条路：有 state_to_dict(..., core_only=True) 做训练最小状态，有 legal_actions 做合法步掩码，有 PlayerSource.select_action(state, legal_actions) 做统一推理入口，还有 headless EVE 和 JSONL trace 可导出。

流程和AI接口总结



流程和AI接口总结

为什么不是 MLP（多层感知机）？可以做，但不优雅。MLP 会把 15×15 棋盘拍平成一长条，空间关系全靠它自己瞎悟，像让人闭着眼学几何，能学，但笨。为什么不是 Transformer？因为第一版太重了，数据量、实现复杂度、调参成本都更高，而你现在最需要的是一个Codex 好实现、能稳定训起来、能作为 ModelAISource 接进去的东西，不是造一个学术仪式现场。再加上你的项目文档里本来就建议当前阶段别急着把复杂训练框架狠狠干进主仓库，这个判断很对。

AI接入可行方案

所以我的推荐是三层递进，但第一版只做第一层。

第一层，也就是你现在最该做的，是：

小型二维 CNN policy-only 模型。

输入不是 JSON 字符串，而是把状态编码成若干个 15×15 的平面。比如最实用的一版可以这样设计：

黑棋位置 1 个平面。
白棋位置 1 个平面。
当前玩家是谁 1 个平面，整盘全填 0 或 1。
当前阶段 4 个 one-hot（独热）平面，阶段 1/2/3/4 四选一。
当前模式 1 个平面，fast/slow。
当前合法落点 mask 1 个平面。
当前可落子区域 1 个平面。
可选：阶段 4 的三连计分相关辅助平面，先不加也行。

这样大概是 9 到 11 个输入通道。然后进一个很小的 CNN，比如：

输入 C x 15 x 15
接 4 到 6 层 3x3 conv + ReLU
通道数 32 或 64
最后输出一个 15x15 分数图
再把非法动作位置用 legal_actions mask 掉
最后在合法位置里取 argmax（最大值位置）

这个模型特别适合你的项目，因为它天然输出“每个坐标有多值得下”，而你的动作本来就是 (row, col)。也就是说，模型输出空间和棋盘坐标是一一对应的，工程上非常顺滑。

我会更具体地推荐这一版：

模型 v1：TinyPolicyCNN

输入通道：10 左右

主干：6 层 Conv2d(kernel=3, padding=1, channels=64) + ReLU

输出：1 x 15 x 15 logits（每个格子的原始分数）

推理：把非法动作置成负无穷，只在合法动作中 softmax 或 argmax

这个版本的好处是：
一，Codex 很容易写对。
二，训练脚本也不复杂。
三，和你的 legal_actions / state_to_dict / ModelAISource 接口天然贴合。
四，它已经能学到很多局部形状，比如连子、堵点、边缘暴露、出锋附近危险区这些“图案味”的东西。

第二层，等你第一版跑起来以后，再考虑：

Policy + Value 双头 CNN。

这里的 policy head 还是输出每个格子的动作分数；value head 则输出当前局面对当前玩家的价值，比如 [-1, 1] 的一个数，表示“这盘大概是赢还是输”。这就更像 AlphaZero 味道了，但我不建议你第一天就做。原因是你当前更需要“会选动作”，而不是“还能评价局面未来胜率”。不过等你进入第三步自博弈擂台时，value head 会变得很有用，因为它能帮助模型学长程伏笔，而不只是模仿老师动作。

第三层，真的到了后期再说：

CNN + 搜索（比如轻量 MCTS）
或者
更深的残差 CNN（ResNet 风格）

这一步现在别碰。先别让自己同时背棋类环境、监督学习、自博弈、搜索树、价值网络五座山。宇宙已经够荒诞了，没必要主动加码。

所以如果你问我一句最硬的结论：

第一版模型就用小型 2D CNN，输出 15×15 动作分数图。

不是 MLP，不是 Transformer，不是 LLM，不是“让模型生成坐标文本”。

再说训练目标，也要尽量土而有效。

第一版训练只做 policy supervised learning（监督策略学习）。也就是：
输入状态，
输出 225 个位置分数，
标签是老师 AI 实际下的那一步。

训练时只在合法动作里计算损失。非法动作直接 mask 掉。这非常重要，因为你的阶段式棋盘并不是全盘一直可下，阶段开放区域会变，规则里写得很清楚。

README



README


如果不 mask，模型会学出一堆“理论上很香、实际上压根不合法”的电子胡话。

输入编码这块，我建议你就让 Codex 按这个来做，不要让它自由发挥：

board_black[15,15]：黑子为 1，否则 0
board_white[15,15]：白子为 1，否则 0
current_player[15,15]：当前玩家是黑填 1，否则 0；或用两个 plane 也行
stage_1 ... stage_4[15,15]：四个阶段 one-hot 平面
mode_fast[15,15]：fast 为 1，否则 0
legal_mask[15,15]：当前合法动作位置为 1
playable_region[15,15]：当前阶段允许区域为 1

这就够用了。你甚至可以一开始不把“出锋核心状态”专门拆成平面，因为真实棋形里已经隐含了大部分信息；后面如果效果不够，再加辅助特征。你文档里也说了 core_only=True 会保留训练必要字段和出锋核心复现状态，这很适合后续慢慢增强，但第一版别贪。

AI接入可行方案

至于框架，现实一点：

训练用 PyTorch，放在独立训练目录或脚本里，不要深深嵌进主游戏循环。

这和你现有方案是对齐的：主仓库先保证规则正确、接口稳定，复杂训练框架不要一开始就绑死进去。

AI接入可行方案


也就是说，游戏项目继续负责：

产生 trace

提供 state serialization

提供 ModelAISource 加载权重做推理

而训练脚本单独负责：

读 JSONL

编码状态

训练模型

保存 policy_v1.pt

这样最干净，不容易把项目煮成稀饭。

我给你一个非常简练的最终推荐表：

现在立刻做：
TinyPolicyCNN，6 层 3×3 卷积，64 通道，输出 15×15 动作分数图。

第一轮别做：
Transformer、MCTS、value head、纯强化学习。

第二轮再加：
Policy + Value 双头 CNN。

第三轮再看：
模型提名候选动作，规则/搜索复核前 K 个动作。

一句话定案：

你的棋第一版最合适的模型，不是“会说话的模型”，而是“会看棋盘图案并给每个坐标打分的小 CNN”。

这玩意儿既符合棋类本质，也最适合让 Codex 实现。

我可以下一条直接把这个模型写成一份 给 Codex 的精确规格：输入张量定义、PyTorch 类结构、dataset 格式、loss 写法、推理时合法步 mask 逻辑、以及 ModelAISource 怎么接进你现有 PlayerSource。