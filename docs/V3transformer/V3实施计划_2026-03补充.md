# V3实施计划补充（2026-03）

## 1. 空桶兜底（已落地）
1. `scripts/curate_v3_dataset.py` 新增 `--missing-bucket-policy`。
2. 默认值为 `drop`，即不再因为单个空桶直接中断整轮。
3. 安全约束保留：活跃桶必须同时覆盖黑白两方，否则仍中断。

## 2. 稳启动默认参数（已落地）
1. `scripts/run_v3_league_cycle.py` 默认对手池调整为前期 teacher/baseline 更重。
2. 默认 `v3-decision-beta` 下调，避免弱 value 在早期放大噪声。
3. 默认长度分段调整为 `split-a=25, split-b=40`，更贴近阶段边界。

## 3. 稳定起步顺序（执行建议）
1. 先训练 `model_v3_shared_bootstrap.pt`。
2. 复制为 `model_v3_best_black.pt` 和 `model_v3_best_white.pt`。
3. 使用 `run_v3_league_cycle.py` 新默认继续联赛，并保留 baseline 门槛。

## 4. 强度不足时的下一步
1. 用 V2 best 作为“数据老师”，导出高质量对局轨迹给 V3 学习。
2. 不做 V2->V3 直接权重加载（结构不兼容），只做行为蒸馏。
